# Server Configuration
PORT=8080

# Database Configuration – Option 1: Full connection URL
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# Database Configuration – Option 2: Individual variables (used when DATABASE_URL is not set)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=daas_test_app
DB_USER=postgres
DB_PASSWORD=postgres

# CORS
FRONTEND_URL=http://localhost:3000

