# Backend URL accessible from the browser at runtime
# In production, this is injected via docker-entrypoint.sh -> runtime-config.js
BACKEND_URL=http://localhost:8080

# Vite build-time fallback (used only in development)
VITE_BACKEND_URL=http://localhost:8080

